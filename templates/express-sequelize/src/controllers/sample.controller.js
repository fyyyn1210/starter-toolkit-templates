import { body, validationResult, query, param } from 'express-validator';
import { Op } from 'sequelize';
import logger from '#src/configs/winston.config.js';

const SampleController = {

};

export default SampleController;
